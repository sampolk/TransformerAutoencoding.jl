# TransformerAutoencoding.jl
TransformerAutoencoding.jl provides Julia utilities for sequence-to-sequence autoencoding of variable-length data using lightweight Flux Transformer blocks. It supports padded minibatches and masked losses, enabling embeddings for clustering, retrieval, anomaly detection, and downstream models.
